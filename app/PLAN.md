# PLAN.md
> 记录后续要做什么，只聚焦实现目标、验收条件与限制条件。
> 不记录已完成历史，不记录当前临时断点。
---

## 当前执行阶段

Phase 5 - fd_reportai_word 适配器

---

## Phase 1 - 页面骨架

### 实现目标

- 搭建可运行的基础页面结构
- 完成左侧模版树 / 中间预览 / 右侧编辑区 / 下方对话区的四区布局
- 使用 mock 数据打通页面展示链路
- 建立基础的 `ReportSession` 或页面状态对象

### 验收条件

- 页面可以正常启动
- 四区布局完整可见
- 左侧可展示 mock 模版树
- 右侧可展示 mock 字段
- 中间可展示 mock 预览内容
- 下方可输入对话内容
- 页面整体无报错

### 限制条件

- 不接入真实模版配置
- 不实现真实字段映射
- 不接入文档生成
- 不引入 LangGraph
- 不实现导出能力

---


## Phase 2 - 模版结构读取

### 实现目标

- 将左侧模版树切换为真实配置数据源
- 打通 `fd_reportai_word -> ReportSession.template_tree -> sidebar_tree`
- 保留 mock fallback，保证页面在依赖不可用时仍可运行

### 验收条件

- 左侧展示真实模版结构
- 节点点击与切换正常
- 页面无报错
- `fd_reportai_word` 不可用时，自动回退到 mock 数据

### 限制条件

- 不实现字段映射
- 不修改整体 UI 布局
- 不引入 LangGraph
- 不改造 chat panel
- 不接入文档生成和导出

---

## Phase 3 - 要素系统

### 实现目标

- 建立节点与字段的映射关系
- 为右侧编辑区提供真实字段数据
- 将字段修改写回 `ReportSession`

### 验收条件

- 选中节点后，右侧展示对应字段
- 字段保存后，session 中的数据同步更新
- 中间预览可基于字段更新

### 限制条件

- 不生成最终文档
- 不接入对话编排
- 不实现文件上传解析

---

## Phase 4 - 文档预览

### 实现目标

- 支持节点级预览
- 支持全文预览切换
- 让预览内容脱离纯 mock 文本

### 验收条件

- 可在节点预览与全文预览之间切换
- 预览内容与当前 session 数据一致
- 页面刷新后预览状态正常

### 限制条件

- 不导出 docx
- 不接入 AI 对话生成

---

## Phase 5 - 文档生成

### 实现目标

- 接入 `fd_reportai_word`
- 生成节点级或全文 markdown
- 建立从 session 到生成结果的主链路

### 验收条件

- 可生成 markdown 结果
- 生成结果可在预览区展示
- 生成过程无阻塞性报错

### 限制条件

- 不实现对话式修改
- 不实现 docx 导出

---

## Phase 6 - 对话能力

### 实现目标

- 接入 graph 层
- 让对话可以驱动字段修改
- 支持通过对话触发内容生成

### 验收条件

- 用户输入可进入对话流程
- 对话结果可更新字段或预览内容
- 对话记录可保存在 session 中

### 限制条件

- 不处理文件上传解析
- 不在本阶段处理导出链路

---

## Phase 7 - 文件上传

### 实现目标

- 实现文件上传后的解析入口
- 接入 `file_parser`
- 将解析结果写入可用状态

### 验收条件

- 可上传文件
- 可触发解析流程
- 解析结果可被后续流程使用

### 限制条件

- 不在本阶段优化复杂文件格式兼容性
- 不在本阶段处理导出问题

---

## Phase 8 - 导出

### 实现目标

- 接入 `pypandoc`
- 支持导出 docx
- 提供可下载的导出结果

### 验收条件

- 可生成 docx 文件
- 用户可下载导出结果
- 导出失败时有明确提示

### 限制条件

- 不在本阶段重构前面阶段逻辑
- 不扩展额外导出格式

---

## 最终目标

- 可浏览真实模版结构
- 可编辑节点字段
- 可预览节点与全文内容
- 可通过对话修改内容
- 可导出 docx
