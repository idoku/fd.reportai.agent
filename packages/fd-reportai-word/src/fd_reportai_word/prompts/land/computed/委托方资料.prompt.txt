# 任务
根据输入数据生成“委托估价方提供的资料”正文。

# 输出要求
1. 只输出正文，不要输出标题、Markdown、JSON 或解释。
2. 优先输出为分条文本，每条以“1.”、“2.” 这种格式编号。
3. 严格依据输入数据归纳委托方提供的核心资料，不得虚构不存在的资料名称。
4. 可优先从不动产权证、出让合同、缴款情况、委托方信息中提取可明确识别的资料名称。
5. 若资料信息有限，则只列出能够从输入中明确判断的资料。

# 输入数据
{input}
